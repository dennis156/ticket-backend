var log = require('wizzi-core').log(module);
var verify = require('wizzi-core').verify;
var ittf = require('./lib/ittf/ittf');
var printer = require('./lib/ittf/printer');
var utilnode = require('./lib/util/node');
// var repo = require('./lib/repo');

var md = module.exports = {};

md.loadModel = function (uri, context, callback) {
    ittf.load(uri, context, callback);
}

md.loadModelFromString = function (content, context, callback) {
    ittf.loadFromString(content, context, callback);
}

md.printer = function () {
    return printer;
}

md.utilnode = utilnode;