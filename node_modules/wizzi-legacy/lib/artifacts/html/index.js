var html_module = require('./document/artifact');

module.exports = {
    /**
     * Create a wizzi artifact that targets 
     * the html language.
     *
     * @param  {String} the artifact name
     */
    create: function (name) {
        if (name == 'html/document') return new html_module();
        return null;
    }
};